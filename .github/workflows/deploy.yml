name: Deploy to Hostinger

on:
  push:
    branches:
      - main
  workflow_dispatch:   # Manual trigger button bhi hoga GitHub Actions tab mein

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    name: FTP Deploy to Hostinger
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps        # Hostinger FTPS support karta hai — zyada reliable
          port: 21
          server-dir: public_html/
          local-dir: ./
          timeout: 120000
          log-level: standard
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            api/config.php
            uploads/**
            database/set-admin-password.php
            database/wp-rest-import.php
            database/wp-xml-import.php
            database/wordpress-posts.xml
            test-site/**
            .github/**
            .vscode/**
            .DS_Store
